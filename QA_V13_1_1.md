# QA — v13.1.1

- Corrected GeoTD train-model heading: SVG front/cab is left-facing natively, so the renderer now applies a 180° intrinsic heading correction relative to the movement tangent.
- Added geographic path endpoint validation: the path is forced to run current/previous station → next station before the real-size body and front are calculated.
- Per-train click handling is unchanged.
- HKT/service-day logic is unchanged.
- JavaScript syntax, Python syntax, local serving, and ZIP integrity checked.
